<a href="{{ target }}"
   class="inline-flex items-center border-b-2 {{ active ? 'border-indigo-500' : 'border-transparent' }} px-1 pt-1 text-sm font-medium text-gray-500 hover:border-gray-300 hover:text-gray-700">
    {{ name }}
</a>